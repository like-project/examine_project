<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.goodwill.examine.mapper.DepartMeetingMapper">
    <insert id="insert" parameterType="com.goodwill.examine.entity.DepartMeeting">
        INSERT INTO depart_meeting (topic, hospital_id, images, create_time, update_time)
        VALUES (#{topic}, #{hospitalId}, #{images}, #{createTime}, #{updateTime})
    </insert>
    <select id="selectById" parameterType="int" resultType="com.goodwill.examine.entity.DepartMeeting">
        SELECT * FROM depart_meeting WHERE id = #{id}
    </select>
    <update id="update" parameterType="com.goodwill.examine.entity.DepartMeeting">
        UPDATE depart_meeting
        SET topic = #{topic}, hospital_id = #{hospitalId}, images = #{images}, update_time = #{updateTime}
        WHERE id = #{id}
    </update>
    <delete id="deleteById" parameterType="int">
        DELETE FROM depart_meeting WHERE id = #{id}
    </delete>
</mapper>